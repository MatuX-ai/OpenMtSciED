# 常见问题 (FAQ)

## Q: 为什么路径推荐很慢？
A: 首次运行时系统需要初始化知识图谱连接。后续请求会通过缓存加速。

## Q: 硬件项目烧录失败怎么办？
A: 请确保浏览器支持 WebUSB，并使用 Chrome 或 Edge 浏览器。检查 USB 线是否仅支持充电。

## Q: 如何添加新的课程资源？
A: 运行 `scripts/parsers/` 下的解析器脚本，提取新资源的元数据并导入 Neo4j。

## Q: 可以在没有互联网的环境下使用吗？
A: 可以。通过 Docker 部署后，所有核心功能（包括 AI 导师）均可在离线环境下运行（需提前下载模型）。
